
// This file can be removed if not needed, or used for a specific layout for the /app route.
export default function AppRouteLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
